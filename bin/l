#!/bin/bash
# l -- simple wrapper script to view a command's output in the pager

# pager
VIEWER=${PAGER:-$(which less)}
VIEWER=${VIEWER:-$(which more)}

# main
exec "$@" | $VIEWER

# all pau!   )
exit $?
